name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: obsnotebooks/obsnbsetup/.github/workflows/deploy.yml@main
    permissions:
      pages: write
      id-token: write
